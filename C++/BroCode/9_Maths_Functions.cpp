#include <iostream>
#include <cmath> 

int main() {

    double x = 3.14;
    double y = 4;
    double z;

    z = std::max(x, y); // displays the greater number of two variables
    z = std::min(x, y); 
    z = pow(2, 3); // 2 to the power of 3
    z = sqrt(9) // returns 3.
    z = abs(-7); // how far it is from 0. Therefore returns positive 7
    z = round(x) // returns 3.
    z = ceil(x) // round up to 4
    z = floor(x) // round down to 3

    return 0;

}