
// Array = variable that can store multiple values

let fruit = ["apple", "orange", "banana"];

fruit.push("lemon"); // add element to end
fruit.pop(); // removes last element
fruit.unshift("melon"); // add element to beginning
fruit.shift(); // removes element from beginning

let length = fruit.length; //length of array
let index = fruits.indexOf("banana"); // shows which index the specific element is in